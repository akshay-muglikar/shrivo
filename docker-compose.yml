services:

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://akshay:AE230NGPR3TGw9TxibuLui3Y5Qiv1ovn@dpg-d7bout4vjg8s73c1ohhg-a.singapore-postgres.render.com/inventory_4s8v
      SECRET_KEY: replace-with-a-long-random-secret
      ENVIRONMENT: production
      PORT: 8000
      CORS_ORIGINS: '["http://localhost", "https://${DOMAIN:-localhost}"]'
      ACCESS_TOKEN_EXPIRE_DAYS: ${ACCESS_TOKEN_EXPIRE_DAYS:-30}

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${APP_PORT:-80}:80"
    depends_on:
      - backend
